# Redis Helm values
# Chart: bitnami/redis

# アーキテクチャ（standalone）
architecture: standalone

# 認証無効（docker-composeと同じ）
auth:
  enabled: false

# Master設定
master:
  # Persistence
  persistence:
    enabled: true
    size: 8Gi

  # リソース制限
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

# メトリクス（redis-exporter）
metrics:
  enabled: true
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 128Mi
